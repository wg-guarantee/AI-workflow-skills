## Host Tool Mapping

Claude-oriented skill language may refer to:

- `Skill` for loading another skill
- `Task` for subagents
- `TodoWrite` for checklist or plan tracking
- `Read`, `Write`, `Edit`, and `Bash` for local work

