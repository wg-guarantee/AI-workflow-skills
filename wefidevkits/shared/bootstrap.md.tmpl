# wefidevkits Bootstrap for {{HOST_NAME}}

This is the session bootstrap for `wefidevkits`.

- Load `wefi-intake` before the first substantive response whenever the host supports bootstrap injection.
- Route new feature or behavior-change work to `wefi-scope`.
- Route implementation planning to `wefi-sequence`.
- Route any completion claim through `wefi-exit-check`.
- User instructions always take precedence over skill guidance.

Host bootstrap support: `{{HOST_HAS_SESSION_BOOTSTRAP}}`
